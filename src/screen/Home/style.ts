import React from "react";
import styled from 'styled-components/native';
import { LinearGradient } from "react-native-linear-gradient";

export const ColorContainer = styled(LinearGradient)`

    flex: 1;

`
