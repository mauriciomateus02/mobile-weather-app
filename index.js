/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import sky from './src/screen/Home/home';

AppRegistry.registerComponent(appName, () => sky);
